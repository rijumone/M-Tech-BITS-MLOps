[project]
name = "m-tech-bits-mlops"
version = "0.1.0"
description = ""
authors = [
    {name = "Riju",email = "mailmeonriju@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "loguru (>=0.7.3,<0.8.0)",
    "pytest (>=8.3.4,<9.0.0)",
    "flake8 (>=7.1.1,<8.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
